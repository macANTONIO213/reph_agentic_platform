Django>=5.2,<6.0
python-dotenv>=1.0
anthropic>=0.50
openai>=1.0
boto3>=1.34
gunicorn>=21.2
whitenoise>=6.6
dj-database-url>=2.1
psycopg2-binary>=2.9
# Phase C — Data & Knowledge Layer
pgvector>=0.3          # Postgres vector similarity (gracefully skipped on SQLite)
tiktoken>=0.7          # Token-aware chunking for RAG
pypdf>=4.0             # PDF ingestion
python-docx>=1.1       # DOCX ingestion
sqlalchemy>=2.0        # C3 SQL connector
# Phase 0 — Durable execution
celery[redis]>=5.4     # Durable async execution backend (workflows + A2A tasks)
redis>=5.0             # Celery broker / result backend
cryptography>=43.0    # Fernet encryption for DataConnector.config at rest (GV-2)
mozilla-django-oidc>=5.0   # Enterprise SSO via OIDC (UX-3, env-gated)
