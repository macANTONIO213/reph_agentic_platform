# Dependency pins for reproducible builds (supply-chain control C6).
# Install with:  pip install -r requirements.txt -c constraints.txt
# Regenerate after a deliberate upgrade:  pip freeze > constraints-candidate.txt,
# review the diff, run the test suite, then update this file.
anthropic==0.103.1
celery==5.6.3
dj-database-url==3.1.2
Django==5.2.15
gunicorn==26.0.0
openai==2.41.0
pgvector==0.4.2
pypdf==6.11.0
python-docx==1.2.0
python-dotenv==1.2.2
redis==6.4.0
requests==2.34.2
SQLAlchemy==2.0.50
tiktoken==0.13.0
urllib3==2.7.0
whitenoise==6.12.0
# Prod Postgres driver + AWS SDK — pinned for reproducible builds (audit A-08/DO-03).
# Bump deliberately via the regeneration workflow above; verify on the prod platform.
boto3==1.35.99
psycopg2-binary==2.9.10
cryptography==49.0.0
mozilla-django-oidc==5.0.2
